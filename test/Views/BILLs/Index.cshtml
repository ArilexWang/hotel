@model IEnumerable<test.BILL>


@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.COLUMN1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.COLUMN1)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.COLUMN1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ITEM_TYPE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ITEM_TYPE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ITEM_TYPE })
        </td>
    </tr>
}

</table>
